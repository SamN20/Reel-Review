import { SectionPlaceholderPage } from "../components/SectionPlaceholderPage";

export default function LeaderboardsPage() {
  return (
    <SectionPlaceholderPage
      activeSection="leaderboards"
      eyebrow="Leaderboards"
      title="Rankings deserve a full page."
      description="Leaderboards are intended to live in their own destination, separate from the home landing page, with room for all-time favorites, divisive picks, and community-driven standings. This placeholder route keeps the header aligned with that architecture."
    />
  );
}
