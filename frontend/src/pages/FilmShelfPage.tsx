import { SectionPlaceholderPage } from "../components/SectionPlaceholderPage";

export default function FilmShelfPage() {
  return (
    <SectionPlaceholderPage
      activeSection="film-shelf"
      eyebrow="The Film Shelf"
      title="The archive gets its own stage."
      description="The Film Shelf is planned as a dedicated archive experience with dynamic rows, personalized sorting, and catch-up paths through past drops. This route is here now so the navigation matches the product structure while that full page is built."
    />
  );
}
